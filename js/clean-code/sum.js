/**
 * @description return sum of two numbers
 * @param {Number} first
 * @param {Number} second
 * @returns {Number}
 */
function sum(first, second) {
    return first + second
}

console.log(sum(2,3))